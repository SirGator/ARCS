{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "arcs.event.v1",
  "type": "object",
  "required": [
    "event_id",
    "event_type",
    "ts",
    "actor",
    "refs",
    "stream_key"
  ],
  "properties": {
    "event_id": { "type": "string", "minLength": 1 },
    "event_type": {
      "type": "string",
      "enum": [
        "artifact_committed",
        "head_advanced",
        "approval_recorded",
        "action_materialized",
        "execution_recorded"
      ]
    },
    "ts": { "type": "string", "format": "date-time" },
    "actor": {
      "type": "object",
      "required": ["actor_type", "id"],
      "properties": {
        "actor_type": {
          "type": "string",
          "enum": ["human", "system", "model", "executor"]
        },
        "id": { "type": "string", "minLength": 1 }
      },
      "additionalProperties": false
    },
    "refs": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["artifact_id", "version_id", "role"],
        "properties": {
          "artifact_id": { "type": "string", "minLength": 1 },
          "version_id": { "type": "string", "minLength": 1 },
          "role": {
            "type": "string",
            "enum": ["target", "parent", "policy", "action", "result"]
          }
        },
        "additionalProperties": false
      }
    },
    "stream_key": { "type": "string", "minLength": 1 },
    "payload": { "type": "object" },
    "prev_hash": { "type": "string" }
  },
  "additionalProperties": false
}
