add_library(arcs_schema
    src/schema_loader.cpp
    src/schema_registry.cpp
    src/validator.cpp
)

target_include_directories(arcs_schema
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}/include
)

find_package(nlohmann_json CONFIG REQUIRED)

target_link_libraries(arcs_schema
    PUBLIC
        nlohmann_json::nlohmann_json
)